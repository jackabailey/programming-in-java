# CircleApp

This application has a class to define a circle.
It also contains a class to define the custom exception 'IllegalArgumentException'

The application introduces more complex features of Java, such as custom exceptions and stronger error handling with arguments